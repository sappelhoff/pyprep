dist: trusty

sudo: required

language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
  - pip install -e .

script:
  - pytest test/ -v --cov=./
  - make check

after_success:
- codecov
